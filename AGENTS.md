# 청소의 고수 작업 규칙

## 기준 경로와 문서

- 현재 실제 웹앱 기준 경로는 `C:\Users\yeonw\Documents\카카오톡 받은 파일\clean-master-care-farm-share-20260825`다.
- 제품 요구사항의 기준은 루트 `PRD.md`이며, 구현 순서는 `docs/DEVELOPMENT_PRIORITY.md`를 따른다.
- Supabase 청소 지식 저장소와 검색·LLM 연동의 기준 설계는 `docs/CLEANING_KNOWLEDGE_ERD_AND_PLAN.md`다.
- 문서가 충돌하면 사용자의 최신 명시 요청, 최신 PRD, 위 설계 문서 순으로 판단하고 차이와 결정을 개발일지에 기록한다.

## 작업 시작

작업 전에 `PRD.md`, 이 파일, `docs/DEVELOPMENT_PRIORITY.md`, `docs/dev-logs/INDEX.md`와 최신 개발일지를 읽는다. 사용자 요청이 PRD와 충돌하면 사용자의 최신 명시 요청을 우선하고, 차이를 작업일지에 기록한다.

## 작업 단위와 개발일지

- 작업 단위는 구현·테스트로 검증할 수 있는 한 가지 사용자 가치로 정한다.
- 작업 단위가 구현과 테스트를 통과하면, 다음 작업으로 넘어가기 전에 `docs/dev-logs/YYYY-MM-DD-NNN-슬러그.md`를 만든다.
- 개발일지는 반드시 `무엇을 만들었는지`, `문제·막힌 지점`, `해결`, `다음 작업 시 참고`를 포함한다.
- `docs/dev-logs/INDEX.md`에 날짜·제목·파일 링크를 추가한다.
- 다음 세션은 최신 일지와 INDEX를 먼저 읽고 이어서 진행한다.

## 개인정보·사진

- 사용자 사진 원본을 서버 파일·데이터베이스·로그·localStorage에 저장하지 않는다.
- 사진 비교 중복 방지가 필요하면 복원할 수 없는 해시만 브라우저 localStorage에 저장할 수 있다.
- 청결 점수, 사람의 성실성·정신건강·위생 수준 판단은 구현하지 않는다.

## 청소 지식 시스템의 목표

- 한국 주거 환경의 공간·대상·부위·오염/과업별 청소 절차와 단계별 요령을 검수된 지식으로 제공한다.
- 현재 `static/app.js`의 `buildCarePlan()`에 흩어진 하드코딩 안내를 서버의 청소 지식 API로 점진적으로 대체한다.
- Supabase/Postgres의 정규화된 관계형 데이터가 정답 절차, 순서, 주의사항과 출처의 최종 기준이다.
- pgvector 검색은 정답 절차를 새로 만드는 용도가 아니라, 구조화된 정확 일치로 고른 단계에 보충할 검수 완료 팁을 찾는 용도로만 쓴다.
- 인터넷 문서를 통째로 크롤링해 곧바로 사용자에게 노출하지 않는다. 출처를 수집한 뒤 원문과 분리된 규격화 문안으로 작성하고, 검수·발행된 버전만 검색 대상에 포함한다.

## Supabase 및 지식 데이터 계약

- 다음 Supabase 작업 세션은 `docs/CLEANING_KNOWLEDGE_ERD_AND_PLAN.md`의 K01부터 시작한다. 이번 문서 작업만으로 원격 프로젝트나 테이블이 생성된 것으로 간주하지 않는다.
- 핵심 데이터는 공간, 청소 대상, 시나리오, 절차, 절차 버전, 절차 단계, 단계 팁, 위험 규칙, 출처, 발행 릴리스로 분리한다.
- 앱의 현재 코드값인 `area_hint`, `visible_categories`, `cleaning_focus`를 버리지 말고 라우팅 테이블의 입력 계약으로 유지한다. 새 대상/부위 코드는 별도 정규화 테이블에 매핑한다.
- 발행된 절차 버전은 불변으로 취급한다. 수정은 새 버전을 만들고 릴리스 포인터를 교체하는 방식으로 한다.
- 사용자 제공 사진 원본, 이미지 base64, 자유 입력 원문, 개인정보, OpenAI API 키와 Supabase service-role 키를 청소 지식 테이블이나 임베딩 메타데이터에 저장하지 않는다.
- 임베딩 모델명, 차원, 전처리 버전은 `embedding_profiles`에 고정하고 물리 임베딩 테이블의 vector 차원과 일치시킨다. 모델이나 차원을 바꾸면 별도 프로필과 재색인 작업으로 처리한다.
- 공개 앱 클라이언트는 발행된 읽기 전용 뷰만 조회할 수 있다. 작성·검수·발행·임베딩 생성은 서버 또는 관리자 경로에서만 service-role로 수행하고 브라우저에 service-role 키를 노출하지 않는다.

## 검색 및 라우팅 규칙

1. 사용자가 확정한 `area_hint`, 선택한 `visible_categories`, `cleaning_focus`와 선택 가능한 대상·부위 코드를 서버에서 enum 검증한다.
2. 구조화된 라우팅 테이블에서 현재 발행 릴리스의 정답 절차 버전을 먼저 정확 일치로 찾는다. 더 구체적인 대상·부위 경로가 우선한다.
3. 정확 경로가 없으면 설계 문서에 정의된 상위 범주와 `unknown` 대체 경로를 순서대로 적용한다. 임의의 벡터 유사도만으로 정답 절차를 선택하지 않는다.
4. 정답 절차의 각 단계에 연결된 발행 팁을 먼저 불러오고, 필요한 경우에만 동일 공간·대상·과업·위험 태그로 필터링한 pgvector 검색 결과를 보충한다.
5. 위험 규칙과 중단 조건은 LLM 호출 전에 서버에서 결정적으로 계산한다. LLM은 이를 삭제하거나 완화하거나 재해석할 수 없다.
6. 검색 결과에는 안정적인 ID와 버전(`procedure_version_id`, `step_id`, `tip_version_id`, `risk_rule_version_id`, `source_id`, `release_key`)을 포함해 재현 가능하게 한다.

## LLM 컨텍스트 입력 계약

- OpenAI Responses API에는 서버에서 검증한 사용자 확정값과 발행된 검색 결과만 컨텍스트로 전달한다.
- 사진 분석 모델의 관찰 문구는 `관찰 가능한 후보`로 명시하고, 사용자가 확정하지 않은 후보를 사실처럼 청소 지식 라우팅에 사용하지 않는다.
- LLM 컨텍스트는 최소한 다음 구조를 가진다.

```json
{
  "request": {
    "locale": "ko-KR",
    "area_hint": "sink",
    "visible_categories": ["clean"],
    "cleaning_focus": "water_scale",
    "target_code": "kitchen_sink",
    "part_code": "drain_strainer",
    "user_confirmed": true,
    "observations": ["사진에서 관찰 가능한 표현만"]
  },
  "retrieval": {
    "release_key": "stable",
    "procedure_version": {},
    "steps": [],
    "step_tips": [],
    "risk_rules": [],
    "sources": []
  }
}
```

- 컨텍스트에는 사용자 사진 원본이나 base64를 다시 넣지 않는다. 사진은 기존 `/api/analyze` 요청 처리 중 메모리에서만 사용한다.
- 프롬프트는 검색된 지식을 유일한 청소 사실 근거로 선언하고, 근거가 없는 세부 사항을 보충하지 말도록 명시한다.
- 앱의 모델명은 코드와 PRD가 일치하도록 한 곳에서 설정한다. 현재 `server.py`와 PRD의 모델 표기가 다르므로 다음 구현 세션 첫 작업에서 확인하고 개발일지에 결정 근거를 남긴다.

## LLM 출력 및 서버 검증 계약

- LLM은 검색된 발행 절차를 사용자의 현재 단계에 맞게 간결하게 설명할 수 있지만, 새로운 청소 사실·위험 규칙·제품 적합성·재질 판정을 만들어서는 안 된다.
- 출력은 자유 텍스트가 아니라 Responses API의 엄격한 JSON Schema로 받는다. 최소 필드는 다음과 같다.

```json
{
  "procedure_version_id": "uuid",
  "release_key": "stable",
  "title": "string",
  "summary": "string",
  "steps": [
    {
      "step_id": "uuid",
      "step_no": 1,
      "stage_code": "prepare",
      "instruction": "string",
      "detail": "string",
      "completion_cue": "string",
      "caution": "string|null",
      "tip_version_ids": ["uuid"],
      "risk_rule_version_ids": ["uuid"],
      "source_ids": ["uuid"]
    }
  ],
  "needs_user_confirmation": false
}
```

- 서버는 출력 스키마, enum, 단계 수와 순서, 모든 참조 ID가 실제 검색 컨텍스트 안에 있는지, 필수 위험 문구가 보존됐는지를 검증한다.
- 욕실 기본 청소처럼 PRD가 6단계를 요구하는 경로는 정확히 6단계와 지정 순서를 보존한다. 다른 생활 돌봄 경로는 발행된 절차 버전의 `required_step_count`와 단계 순서를 보존한다.
- LLM은 제품 브랜드, 검색 컨텍스트에 없는 화학 성분·사용량·희석비·접촉 시간, 사진에서 보이지 않는 사실, 청결 점수, 사용자 평가, 질환·정신건강 추정을 생성하지 않는다.
- PRD가 허용한 곰팡이의 염소계 표백제/곰팡이 제거제 안전 안내가 필요한 경우에도 발행된 위험 규칙과 제품 라벨 준수 문구만 사용하며, LLM이 임의의 비율·시간·재질 적합성을 추가하지 않는다.
- 검증 실패, 빈 검색 결과, Supabase 장애 또는 OpenAI 실패 시 LLM 문장을 사용자에게 부분 노출하지 않는다. 현재 발행 절차의 원문, 번들된 최소 로컬 스냅샷, 기존 수동 선택 경로 순으로 대체한다.

## 새 청소 안내 API의 책임

- 새 서버 API의 기준 경로는 `POST /api/cleaning-guide`로 한다. `/api/analyze`는 사진에서 관찰 가능한 후보만 반환하는 현재 책임을 유지한다.
- `/api/cleaning-guide`는 입력 enum 검증, 발행 릴리스 선택, 정확 라우팅, 단계 팁 검색, 위험 규칙 적용, 선택적 LLM 설명, 최종 스키마 검증과 대체 응답을 한 경계에서 수행한다.
- 프런트엔드가 Supabase를 직접 검색하거나 임의 프롬프트를 조합하지 않게 한다.
- 응답에는 `mode`를 포함한다. 값은 최소 `llm_grounded`, `canonical`, `local_fallback` 중 하나이며 UI는 어느 모드에서도 청소 진행을 막지 않는다.
- 로그에는 원문 컨텍스트와 모델 전체 응답을 남기지 않는다. 릴리스 키, 사용한 레코드 ID, 처리 시간, 오류 코드처럼 복구 가능한 개인정보가 아닌 진단 정보만 남긴다.

## 품질 및 회귀 기준

- 지식 데이터는 최소 두 종류의 검증을 거친다: 스키마/금지어 자동 검사와 사람의 출처·안전·실행 가능성 검수.
- 검색 평가는 공간·대상·부위·과업별 골든 질의를 두고 exact-route 적중률, 잘못된 절차 선택률, 필수 위험 문구 누락률, 출처 없는 문장 비율을 측정한다.
- LLM 평가는 동일 입력 반복 시 단계 ID·순서·위험 규칙이 유지되는지, 검색되지 않은 사실이 추가되지 않는지, 장애 시 결정적 대체 응답이 나오는지 확인한다.
- `static/app.js`의 기존 `buildCarePlan()`은 서버 API와 로컬 대체 경로가 검증되기 전에는 제거하지 않는다. 교체 시 네트워크 실패 회귀 테스트를 먼저 추가한다.
- Supabase 연결 정보나 OpenAI 키가 없는 개발 환경에서도 기존 사진 분석 수동 경로와 청소 미션이 동작해야 한다.

## 검증

- 프런트엔드 수정 뒤 JavaScript 문법 검사를 실행한다.
- 백엔드 수정 뒤 `py -m pytest -q`를 실행한다.
- 화면 변경은 360px 수준의 모바일 레이아웃과 기본 데스크톱 레이아웃을 확인한다.
- DB/검색 작업은 마이그레이션 재실행, RLS, 발행 버전만 조회되는지, 잘못된 enum과 미발행 데이터가 차단되는지를 검증한다.
- LLM 연동 작업은 구조화 출력 검증 실패, 출처 ID 위조, 필수 위험 문구 누락, Supabase/OpenAI/API 키 없음 대체 경로를 테스트한다.
